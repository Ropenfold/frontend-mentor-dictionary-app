import React from 'react'
import styles from './startState.module.css'

const StartState = () => {
  return (
    <div className={styles.startStateContainer}>
      <h2 className={styles.heading}>Find your first word...</h2>
      </div>
  )
}

export default StartState